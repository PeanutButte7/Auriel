**For security reasons a file with token for the bot is not included. To add it simply create a file named `config.json` in the root directory and add your token.**
```
{
  "token": "your_token_here"
}
```
